name: Pre-commit

on:
  push:

jobs:
  pre-commit:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: Alfresco/alfresco-build-tools/.github/actions/pre-commit@40f2376b812403b51e0cea49eaff04c1b8d65cfd # v15.7.1
        with:
          auto-commit: "true"
