name: Pre-commit

on:
  push:

jobs:
  pre-commit:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: Alfresco/alfresco-build-tools/.github/actions/pre-commit@9558268b491821a4cfce1f11d4c8f6e56adc8496 # v5.30.0
        with:
          auto-commit: "true"
