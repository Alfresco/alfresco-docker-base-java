name: Pre-commit

on:
  push:

jobs:
  pre-commit:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: Alfresco/alfresco-build-tools/.github/actions/pre-commit@d3953ad171fd95340c9ecad9d1054561bc938cfd # v5.33.0
        with:
          auto-commit: "true"
