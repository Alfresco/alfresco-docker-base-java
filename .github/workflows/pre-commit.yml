name: Pre-commit

on:
  push:

jobs:
  pre-commit:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: Alfresco/alfresco-build-tools/.github/actions/pre-commit@2cb399462d8e86e9f6e75bcf29ce60e263fe0d0e # v8.34.0
        with:
          auto-commit: "true"
