language: ruby

services:
  - docker

env:
  global:
    - java_versions=8,11 
    - suffix=${TRAVIS_BRANCH}
    - registry=quay.io
    - namespace=alfresco

jobs:
  include:
    - stage: All stages
      name: "All stages"
      script:
      - ./scripts/bin/build.sh
